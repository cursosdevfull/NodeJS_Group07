version: '3'

services:
  master:
    container_name: 'maestro'
    build: .
    ports:
      - '8089:8089'
    command: '--host=http://xxx.com --master'

  slave:
    build: .
    command: '--host=http://xxx.com --slave --master-host=maestro'
